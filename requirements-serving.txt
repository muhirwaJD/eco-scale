# Lean dependencies for the SERVING container (API + dashboard + live sim).
# CPU-only torch (no CUDA/triton) and no training/eval-only extras (scipy,
# tensorboard) — this keeps the Docker image small. matplotlib/pandas come in
# transitively via stable-baselines3.
--extra-index-url https://download.pytorch.org/whl/cpu

gymnasium==1.2.3
stable-baselines3==2.8.0
torch==2.12.0+cpu
numpy==2.4.5
pandas==3.0.3

fastapi==0.136.3
uvicorn[standard]==0.49.0
streamlit==1.58.0
