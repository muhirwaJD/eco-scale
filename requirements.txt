# Eco-Scale dependencies (pinned to the versions the champion was trained/served
# with, so the saved model always loads the same way everywhere — local, Docker,
# and Streamlit Cloud).
#
# CPU-only torch (no CUDA/triton) keeps Linux installs — Streamlit Cloud especially
# — small. macOS wheels are CPU-only already, so they use the plain version.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- core RL / simulation ---
gymnasium==1.2.3
stable-baselines3==2.8.0
torch==2.12.0+cpu ; sys_platform != "darwin"
torch==2.12.0 ; sys_platform == "darwin"
numpy==2.4.5

# --- analysis / training utilities ---
pandas==3.0.3
scipy==1.17.1
matplotlib==3.10.9
tensorboard==2.20.0

# --- serving layer (API + dashboard) ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
streamlit==1.58.0

# --- testing ---
pytest==8.2.2
httpx==0.27.0
